# Evita estruturas `node_modules` baseadas em symlinks (`.pnpm/`) que a Next.js
# empacota em Lambdas incorretamente (“invalid deployment package / symlinked directories” no Vercel).
+# Compatível com Vercel e com o trace de `@sparticuz/chromium`/binários.
node-linker=hoisted
