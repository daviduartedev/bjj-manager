/** Faixa branca: sem graus na operação até o professor registar graduação (produto). */
export function isWhiteBeltSlug(slug: string): boolean {
  return slug === "white" || slug === "white_kids";
}
